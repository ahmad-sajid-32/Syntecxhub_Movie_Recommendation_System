# Flask runtime configuration
FLASK_HOST=127.0.0.1
FLASK_PORT=5000
FLASK_DEBUG=false

# Recommender runtime paths
# Relative paths are resolved from the project root.
PROCESSED_MOVIES_PATH=data/processed/movies_metadata.csv
ARTIFACT_DIR=artifacts